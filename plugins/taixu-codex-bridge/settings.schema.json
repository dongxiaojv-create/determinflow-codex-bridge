{
  "type": "object",
  "properties": {
    "codex_path": {
      "type": "string",
      "default": "",
      "description": "官方 Codex CLI 0.153.4 可执行文件完整路径；留空检测常见安装目录。自定义安装请填写 command -v codex 的结果。"
    },
    "proxy": {
      "type": "string",
      "default": "",
      "description": "仅用于 Codex Runtime 的 HTTP(S) 代理，例如 http://127.0.0.1:7890；留空直连。保存后重启 DeterminFlow。"
    }
  }
}
